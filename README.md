# [Course Name] Notes

This repository contains notes for the course **[Course Name]** as part of the Cybersecurity program at Politecnico di Torino.

## Cloning the Repository

To clone this repository along with all its submodules, please use the `--recursive` option:

```bash
git clone --recursive https://github.com/cybersecurity-polito/[repository-name].git
```

This ensures that all submodules (i.e., additional notes or resources) are properly cloned alongside the main repository.

### If You've Already Cloned Without `--recursive`

If you've already cloned the repository without the `--recursive` flag, you can initialize and update the submodules afterward by running:

```bash
git submodule update --init --recursive
```

## Contributing

Feel free to contribute to these notes by submitting pull requests. Ensure that your contributions follow the repository's structure and formatting guidelines.
